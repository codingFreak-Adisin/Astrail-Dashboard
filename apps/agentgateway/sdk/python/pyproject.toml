[project]
name = "astrail"
version = "0.1.0"
description = "Python client for Astrail hosted MCP endpoints."
requires-python = ">=3.9"
authors = [{ name = "Astrail" }]
license = { text = "MIT" }

[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"
